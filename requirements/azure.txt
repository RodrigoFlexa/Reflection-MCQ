# Dependências mínimas para usar só o backend Azure OpenAI (AzureBackend), sem
# torch, vllm nem transformers — a cadeia de import de rmcq.backends.azure vai
# só até base + config + store, e nenhum deles toca em torch.
#
#   python -m venv venv && source venv/bin/activate
#   pip install -r requirements-azure.txt

openai>=1.40          # cliente AzureOpenAI
tqdm>=4.66.0           # barra de progresso (rmcq.store.progress)
tiktoken>=0.7.0        # contagem de tokens; sem ele a contagem vira estimativa
