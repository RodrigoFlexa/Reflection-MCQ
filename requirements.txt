# Instale só o que o(s) backend(s) que você vai usar precisa(m). O núcleo
# (rmcq/backends/base.py, config.py, store.py, o backend stub) não depende de
# nada além da stdlib.

# ---- Backend hf / vLLM: modelos com pesos locais ----
# Instale torch primeiro, com o índice da sua versão de CUDA:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
torch>=2.4.0
transformers>=4.51.0          # >=4.51 é o mínimo para Qwen3
accelerate>=0.34.0
tokenizers>=0.21.0
safetensors>=0.4.5
huggingface_hub>=1.0.0

# vLLM: continuous batching, muito mais rápido para servir a grade inteira.
# Se a instalação falhar, get_backend() cai automaticamente para transformers.
vllm>=0.6.0

# Quantização 4-bit opcional (RMCQ_LOAD_IN_4BIT=1)
# bitsandbytes>=0.43.0

# ---- Backend Ollama: fala com `ollama serve` por HTTP ----
requests>=2.31.0

# ---- Backend Azure: ver requirements-azure.txt ----

# ---- Notebook 08: recuperação e análise do threshold ----
numpy>=1.26.0
pandas>=2.1.0
matplotlib>=3.8.0
scipy>=1.11.0
sentence-transformers>=3.0.0

# ---- Utilitário ----
tqdm>=4.66.0                  # barra de progresso (rmcq.store.progress)
